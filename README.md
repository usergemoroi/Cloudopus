# 🎮 GameDonate - Магазин донатов для онлайн-игр

Полнофункциональный магазин для продажи донатов в онлайн-игры с интеграцией платежной системы Stripe, личным кабинетом и админ-панелью.

## ✨ Функциональность

- 🎯 **Каталог игр** с описанием, логотипами и скриншотами
- 💰 **Система донатов** с различными пакетами (рубли, доллары, евро)
- 🛒 **Корзина** для выбора нескольких донатов
- 💳 **Интеграция Stripe** для безопасных платежей
- 👤 **Личный кабинет** с историей покупок
- 🔍 **Поиск и фильтрация** по жанрам (MMORPG, FPS, MOBA, и т.д.)
- ⭐ **Система рейтингов** и отзывов
- 📱 **Адаптивный дизайн** для всех устройств
- 🔐 **Аутентификация** с NextAuth.js
- 🎨 **Современный UI** с Tailwind CSS

## 🛠 Технический стек

- **Frontend:** Next.js 14 (App Router), React 18, TypeScript
- **Styling:** Tailwind CSS, Radix UI
- **Backend:** Next.js API Routes
- **База данных:** PostgreSQL с Prisma ORM
- **Аутентификация:** NextAuth.js
- **Платежи:** Stripe
- **API игр:** RAWG API

## 📋 Требования

- Node.js >= 18.0.0
- PostgreSQL >= 14
- npm или yarn

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните следующие переменные:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/game_donate_store?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"  # Сгенерируйте: openssl rand -base64 32

# Stripe (получите на https://stripe.com)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# RAWG API (получите на https://rawg.io/apidocs)
RAWG_API_KEY="your-rawg-api-key"

# App Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_CURRENCY="RUB"

# Admin (для первоначальной настройки)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin123"
```

### 3. Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb game_donate_store

# Примените миграции Prisma
npm run db:push

# Заполните базу тестовыми данными
npm run db:seed
```

### 4. Запуск проекта

```bash
# Режим разработки
npm run dev

# Production сборка
npm run build
npm start
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 👥 Тестовые учетные записи

После запуска `npm run db:seed` будет создан администратор:

- **Email:** admin@example.com (или значение из `ADMIN_EMAIL`)
- **Пароль:** admin123 (или значение из `ADMIN_PASSWORD`)

## 📁 Структура проекта

```
game-donate-store/
├── app/                      # Next.js App Router
│   ├── api/                 # API routes
│   │   ├── auth/           # Аутентификация
│   │   ├── cart/           # Корзина
│   │   ├── checkout/       # Оформление заказа
│   │   ├── games/          # Игры
│   │   └── orders/         # Заказы
│   ├── auth/               # Страницы входа/регистрации
│   ├── cart/               # Страница корзины
│   ├── checkout/           # Страница оформления заказа
│   ├── games/              # Каталог и страницы игр
│   ├── profile/            # Личный кабинет
│   ├── globals.css         # Глобальные стили
│   ├── layout.tsx          # Корневой layout
│   └── page.tsx            # Главная страница
├── components/              # React компоненты
│   ├── ui/                 # UI компоненты (shadcn/ui)
│   ├── auth-provider.tsx   # Провайдер аутентификации
│   ├── game-card.tsx       # Карточка игры
│   ├── donate-package-card.tsx
│   ├── header.tsx          # Шапка сайта
│   └── footer.tsx          # Подвал сайта
├── lib/                     # Библиотеки и утилиты
│   ├── prisma.ts           # Prisma client
│   ├── auth.ts             # Конфигурация NextAuth
│   ├── stripe.ts           # Stripe client
│   ├── rawg.ts             # RAWG API client
│   └── utils.ts            # Утилиты
├── prisma/                  # Prisma ORM
│   ├── schema.prisma       # Схема базы данных
│   └── seed.ts             # Seed скрипт
├── types/                   # TypeScript типы
├── public/                  # Статические файлы
└── package.json
```

## 🎮 Начальный каталог игр

После seed'а в базу будут добавлены 15 популярных игр:

- World of Warcraft (MMORPG)
- Counter-Strike 2 (FPS)
- League of Legends (MOBA)
- Dota 2 (MOBA)
- Valorant (FPS)
- Apex Legends (Battle Royale)
- Fortnite (Battle Royale)
- Minecraft (Sandbox)
- Genshin Impact (RPG)
- Rust (Survival)
- Escape from Tarkov (FPS)
- Destiny 2 (MMO FPS)
- Roblox (Sandbox)
- GTA V (Action)
- Final Fantasy XIV (MMORPG)

Каждая игра включает 3 пакета донатов:
- **Starter Pack** (499 ₽)
- **Premium Pack** (1499 ₽) - популярный
- **Ultimate Pack** (2999 ₽)

## 🔧 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev сервера
npm run build            # Production сборка
npm run start            # Запуск production сервера
npm run lint             # Проверка кода

# База данных
npm run db:push          # Применить изменения схемы
npm run db:seed          # Заполнить тестовыми данными
npm run db:studio        # Открыть Prisma Studio
```

## 🌐 Развертывание

### Vercel (Рекомендуется)

1. Загрузите проект на GitHub
2. Импортируйте в Vercel
3. Добавьте переменные окружения
4. Подключите PostgreSQL (например, через Vercel Postgres или Supabase)
5. Deploy!

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new)

### Heroku

```bash
# Установите Heroku CLI
heroku create your-app-name

# Добавьте PostgreSQL
heroku addons:create heroku-postgresql:hobby-dev

# Настройте переменные окружения
heroku config:set NEXTAUTH_SECRET="your-secret"
heroku config:set STRIPE_SECRET_KEY="sk_..."
# ... остальные переменные

# Deploy
git push heroku main

# Запустите миграции
heroku run npm run db:push
heroku run npm run db:seed
```

### Railway / Render

Аналогично Heroku - подключите PostgreSQL и настройте переменные окружения.

## 🔐 Stripe настройка

1. Зарегистрируйтесь на [stripe.com](https://stripe.com)
2. Получите API ключи в Dashboard → Developers → API keys
3. Для тестирования используйте тестовые ключи (`pk_test_...`, `sk_test_...`)
4. Тестовые карты:
   - Успешная оплата: `4242 4242 4242 4242`
   - Требует 3D Secure: `4000 0027 6000 3184`
   - Отклонена: `4000 0000 0000 0002`

## 📸 Получение изображений игр

Проект использует RAWG API для получения данных об играх:

1. Зарегистрируйтесь на [rawg.io](https://rawg.io/apidocs)
2. Получите бесплатный API ключ
3. Добавьте в `.env`: `RAWG_API_KEY="your-key"`

Альтернативы:
- IGDB API (требует Twitch аккаунт)
- Вручную добавить изображения в `/public/games/`

## 🐛 Решение проблем

### Ошибка подключения к БД

```bash
# Проверьте, что PostgreSQL запущен
pg_isready

# Проверьте DATABASE_URL в .env
echo $DATABASE_URL
```

### Ошибки Prisma

```bash
# Переcгенерируйте Prisma Client
npx prisma generate

# Сбросьте базу данных
npx prisma db push --force-reset
npm run db:seed
```

### Проблемы с Stripe

- Убедитесь, что используете правильные ключи (test/live)
- Проверьте, что webhook secret корректный
- Для локальной разработки используйте Stripe CLI

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Создано с ❤️ для изучения современного веб-разработки

## 🤝 Вклад в проект

Принимаются Pull Requests! Для больших изменений сначала откройте Issue.

## 📧 Поддержка

Если у вас возникли вопросы, создайте Issue в GitHub.

---

**Важно:** Это демо-проект для обучения. Для production использования необходимо:
- Настроить proper security headers
- Добавить rate limiting
- Настроить мониторинг и логирование
- Добавить unit и e2e тесты
- Настроить CI/CD
- Добавить резервное копирование БД
