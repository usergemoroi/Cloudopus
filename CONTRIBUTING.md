# 🤝 Contributing to GameDonate

Спасибо за интерес к проекту! Мы приветствуем любой вклад.

## Как внести вклад

### Reporting Bugs

Если вы нашли баг:

1. Проверьте, что баг еще не был зарегистрирован в Issues
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия Node.js и браузера

### Suggesting Features

Для предложения новых функций:

1. Создайте Issue с тегом `feature request`
2. Опишите функцию и её пользу
3. Предложите возможную реализацию

### Pull Requests

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Coding Guidelines

- Используйте TypeScript
- Следуйте существующему стилю кода
- Добавляйте комментарии для сложной логики
- Используйте осмысленные имена переменных
- Следуйте принципам SOLID

### Commit Messages

Используйте conventional commits:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: add payment confirmation email
fix: resolve cart total calculation bug
docs: update deployment instructions
```

### Development Setup

```bash
# Fork и клонирование
git clone https://github.com/YOUR_USERNAME/game-donate-store.git
cd game-donate-store

# Установка зависимостей
npm install

# Настройка окружения
cp .env.example .env
# Заполните .env

# Запуск БД
npm run db:push
npm run db:seed

# Запуск dev сервера
npm run dev
```

### Testing

Перед отправкой PR убедитесь что:

- [ ] Код компилируется без ошибок (`npm run build`)
- [ ] Нет линтинг ошибок (`npm run lint`)
- [ ] Проект запускается локально
- [ ] Новые функции протестированы

## Вопросы?

Создайте Issue или свяжитесь с maintainers.

Спасибо за ваш вклад! 🎉
