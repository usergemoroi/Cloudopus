# ⚡ Быстрый старт GameDonate

Следуйте этим шагам для быстрого запуска проекта локально.

## 📋 Предварительные требования

Убедитесь, что у вас установлено:
- Node.js 18+ ([скачать](https://nodejs.org/))
- PostgreSQL 14+ ([скачать](https://www.postgresql.org/download/))
- npm или yarn

Проверка версий:
```bash
node --version  # должно быть >= 18.0.0
npm --version
psql --version  # должно быть >= 14
```

## 🚀 Установка за 5 минут

### 1. Клонирование (если еще не сделано)
```bash
git clone <repository-url>
cd game-donate-store
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка базы данных

#### Создайте PostgreSQL базу:
```bash
# Войдите в PostgreSQL
psql postgres

# В psql консоли:
CREATE DATABASE game_donate_store;
CREATE USER gamedonateuser WITH PASSWORD 'devpassword123';
GRANT ALL PRIVILEGES ON DATABASE game_donate_store TO gamedonateuser;
\q
```

### 4. Настройка переменных окружения

Создайте файл `.env`:
```bash
cp .env.example .env
```

Минимальная конфигурация для локального запуска:
```env
# База данных
DATABASE_URL="postgresql://gamedonateuser:devpassword123@localhost:5432/game_donate_store?schema=public"

# NextAuth (сгенерируйте секрет)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-secret-change-in-production-openssl-rand-base64-32"

# Stripe (тестовые ключи)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_51..."
STRIPE_SECRET_KEY="sk_test_51..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# RAWG API (опционально для получения изображений)
RAWG_API_KEY=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_CURRENCY="RUB"

# Админ (для seed)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin123"
```

### 5. Инициализация базы данных
```bash
# Примените схему
npm run db:push

# Заполните тестовыми данными
npm run db:seed
```

### 6. Запуск проекта
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) 🎉

## 🔑 Тестовые учетные данные

После seed'а:
- **Email:** admin@example.com
- **Пароль:** admin123

## 🧪 Тестирование Stripe

Для тестирования платежей используйте:

**Тестовые карты:**
- Успешная оплата: `4242 4242 4242 4242`
- Требует 3D Secure: `4000 0027 6000 3184`
- Отклонена: `4000 0000 0000 0002`

**Данные:**
- CVV: любые 3 цифры
- Дата: любая будущая дата
- Почтовый код: любой

## 📱 Что дальше?

1. **Исследуйте функционал:**
   - Просмотр каталога игр
   - Добавление в корзину
   - Оформление заказа
   - Личный кабинет
   - Админ панель (для админов)

2. **Настройте Stripe:**
   - Зарегистрируйтесь на [stripe.com](https://stripe.com)
   - Получите API ключи
   - Обновите `.env`

3. **Настройте RAWG API (опционально):**
   - Зарегистрируйтесь на [rawg.io](https://rawg.io/apidocs)
   - Получите бесплатный ключ
   - Добавьте в `.env`

4. **Добавьте свои игры:**
   - Через админ панель
   - Или отредактируйте `prisma/seed.ts`

## 🛠 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev сервера (http://localhost:3000)
npm run build            # Production сборка
npm run start            # Запуск production версии
npm run lint             # Проверка кода

# База данных
npm run db:push          # Применить схему
npm run db:seed          # Заполнить данными
npm run db:studio        # Открыть Prisma Studio (GUI для БД)

# Очистка
rm -rf .next             # Очистить кэш Next.js
rm -rf node_modules      # Удалить зависимости
npm install              # Переустановить зависимости
```

## ❓ Проблемы?

### База данных не подключается
```bash
# Проверьте, что PostgreSQL запущен
sudo systemctl status postgresql  # Linux
brew services list                # macOS

# Проверьте подключение
psql -U gamedonateuser -d game_donate_store -h localhost
```

### Порт 3000 занят
```bash
# Найдите процесс
lsof -i :3000

# Или используйте другой порт
PORT=3001 npm run dev
```

### Ошибки Prisma
```bash
# Переcгенерируйте Prisma Client
npx prisma generate

# Сбросьте базу (ВНИМАНИЕ: удалит все данные!)
npx prisma db push --force-reset
npm run db:seed
```

### Stripe не работает
- Используйте тестовые ключи для разработки
- Webhook secret нужен только для webhook'ов
- В dev режиме можно работать без webhook'ов

## 📚 Документация

- [README.md](./README.md) - Полная документация
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Руководство по развертыванию
- [CONTRIBUTING.md](./CONTRIBUTING.md) - Гайд для контрибьюторов

## 🎓 Изучение кодовой базы

Начните с этих файлов:
1. `app/page.tsx` - главная страница
2. `app/games/page.tsx` - каталог игр
3. `app/api/cart/route.ts` - API корзины
4. `prisma/schema.prisma` - схема БД
5. `components/game-card.tsx` - компонент карточки игры

## 💡 Советы

- Используйте **Prisma Studio** для просмотра данных: `npm run db:studio`
- Изучите **Next.js App Router** документацию
- Посмотрите **Tailwind CSS** для стилизации
- Читайте комментарии в коде

## 🎉 Готово!

Теперь у вас запущен полнофункциональный магазин донатов!

Удачной разработки! 🚀
